{
  "name": "my-config",
  "private": true,
  "workspaces": ["packages/**"],
  "scripts": {
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "husky": "^4.3.0",
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5"
  },
  "commitlint": {
    "extends": [
      "./packages/commitlint-config"
    ]
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
